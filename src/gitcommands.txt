-----GIT PUSH (to applied changes )----------------------------
   
    1. git add .
    2. git status
    3. git commit -m "Commit name "
    4. git push origin main 


------GIT PULL(first when start)-------------------------------
    
    1. git pull upstream main or git pull --rebase upstream main

